services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: autocheck-backend
    restart: unless-stopped
    env_file:
      - ./backend/.env.docker
    ports:
      - "8484:8484"

volumes:
  postgres_data:
